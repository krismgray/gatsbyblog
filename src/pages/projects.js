import React, { Component } from 'react';
import Helmet from 'react-helmet';

class Projects extends Component {
  render() {
    return (
      <div>
        <Helmet title="Kris's Projects" />
        These are awesome projects I built!
      </div>
    );
  }
}

export default Projects;
